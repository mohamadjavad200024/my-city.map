# 🚀 استقرار روی Render (برای ایران)

## چرا Render؟

- ✅ معمولاً در ایران بهتر کار می‌کند
- ✅ رایگان برای شروع
- ✅ SQLite را پشتیبانی می‌کند
- ✅ استقرار خودکار از GitHub

---

## مراحل استقرار

### مرحله 1: ثبت‌نام

1. به [render.com](https://render.com) بروید
2. روی **"Get Started for Free"** کلیک کنید
3. **"Sign up with GitHub"** را انتخاب کنید
4. اجازه دسترسی به GitHub را بدهید

### مرحله 2: ایجاد Web Service

1. در داشبورد، روی **"New +"** کلیک کنید
2. **"Web Service"** را انتخاب کنید
3. Repository **`my-city.map`** را انتخاب کنید
4. روی **"Connect"** کلیک کنید

### مرحله 3: تنظیمات

**اطلاعات پایه:**
- **Name**: `city-map` (یا هر نامی که می‌خواهید)
- **Region**: `Frankfurt` یا `Singapore` (نزدیک‌تر به ایران)
- **Branch**: `main`
- **Root Directory**: (خالی بگذارید)

**Build & Deploy:**
- **Runtime**: `Node`
- **Build Command**: 
  ```
  npm install && npm run build
  ```
- **Start Command**: 
  ```
  npm start
  ```

**Plan:**
- **Free** (برای شروع) یا **Starter** (برای production)

### مرحله 4: Environment Variables (اختیاری)

اگر نیاز به متغیرهای محیطی دارید:
- **Key**: `NODE_ENV`
- **Value**: `production`

### مرحله 5: Deploy

1. روی **"Create Web Service"** کلیک کنید
2. منتظر بمانید تا build و deploy شود (3-5 دقیقه)
3. لینک پروژه شما آماده است!

---

## دریافت Domain

بعد از deploy موفق:
- Render به صورت خودکار یک domain رایگان می‌دهد
- مثلاً: `city-map.onrender.com`
- می‌توانید Custom Domain هم اضافه کنید

---

## به‌روزرسانی خودکار

بعد از اولین deploy:
- هر push به GitHub، خودکار deploy می‌شود!
- نیازی به کار دستی نیست

---

## 🔧 تنظیمات SQLite در Render

Render فایل سیستم پایدار دارد، اما:
- فایل‌های دیتابیس در `/opt/render/project/src/database` ذخیره می‌شوند
- اگر نیاز باشد، می‌توانید مسیر را در Environment Variables تنظیم کنید

---

## 🆘 رفع مشکلات

### Build Failed

1. Logs را در Render بررسی کنید
2. مطمئن شوید `npm run build` در local کار می‌کند

### دیتابیس کار نمی‌کند

- Render فایل سیستم پایدار دارد
- دیتابیس به صورت خودکار ساخته می‌شود
- اگر مشکل داشتید، Logs را بررسی کنید

---

## ✨ مزایای Render

- ✅ معمولاً در ایران قابل دسترسی است
- ✅ رایگان برای شروع
- ✅ SQLite کار می‌کند
- ✅ استقرار خودکار
- ✅ Domain رایگان

---

**موفق باشید!** 🚀

