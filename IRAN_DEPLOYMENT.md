# 🚀 راه‌حل‌های استقرار برای ایران

## ⚠️ مشکل Railway

Railway ممکن است در ایران محدود باشد یا IP ایران را بلاک کند.

---

## ✅ راه‌حل‌های جایگزین

### 1. Render (پیشنهادی)

Render معمولاً در ایران بهتر کار می‌کند.

**مراحل:**
1. به [render.com](https://render.com) بروید
2. با GitHub ثبت‌نام کنید
3. "New Web Service" > Repository را انتخاب کنید
4. تنظیمات:
   - Build: `npm install && npm run build`
   - Start: `npm start`
5. Deploy کنید!

---

### 2. VPS ایرانی

استفاده از VPS ایرانی (مثل آریاوب، میزبان فا، و غیره)

**مزایا:**
- ✅ سرعت بالا در ایران
- ✅ بدون محدودیت
- ✅ کنترل کامل

**مراحل:**
1. VPS خریداری کنید
2. Node.js و PM2 نصب کنید
3. پروژه را clone کنید
4. `npm install && npm run build && npm start`

---

### 3. Cloudflare Pages + Supabase

استفاده از Cloudflare Pages (رایگان) + Supabase برای دیتابیس

**مزایا:**
- ✅ رایگان
- ✅ سرعت بالا
- ✅ CDN جهانی

---

### 4. DigitalOcean App Platform

اگر VPN دارید، می‌توانید از DigitalOcean استفاده کنید.

---

## 🎯 پیشنهاد: Render

Render بهترین گزینه برای ایران است:

1. **ثبت‌نام:**
   - به [render.com](https://render.com) بروید
   - با GitHub وارد شوید

2. **ایجاد Web Service:**
   - "New +" > "Web Service"
   - Repository `my-city.map` را انتخاب کنید

3. **تنظیمات:**
   - Name: `city-map`
   - Region: `Frankfurt` یا `Singapore` (نزدیک‌تر به ایران)
   - Branch: `main`
   - Build Command: `npm install && npm run build`
   - Start Command: `npm start`
   - Plan: `Free`

4. **Deploy:**
   - "Create Web Service" را بزنید
   - منتظر بمانید (3-5 دقیقه)

---

## 📝 تغییرات لازم برای Render

هیچ تغییر خاصی لازم نیست! پروژه شما آماده است.

فقط مطمئن شوید که:
- ✅ `package.json` درست است
- ✅ `server.js` وجود دارد
- ✅ Build command: `npm install && npm run build`
- ✅ Start command: `npm start`

---

## 🔄 اگر Railway کار کرد

اگر Railway کار کرد اما فقط در ایران مشکل دارید:
- می‌توانید از VPN استفاده کنید
- یا از Render استفاده کنید که بهتر است

---

**پیشنهاد نهایی: Render را امتحان کنید!** 🚀

