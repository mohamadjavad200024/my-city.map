# 🔧 اصلاح تنظیمات Railway

## ✅ وضعیت فعلی

- ✅ Domain: `my-citymap-production.up.railway.app`
- ✅ Port: 3000 (درست است)
- ✅ Start command: `npm start` (درست است)

## ⚠️ مشکلات

### 1. Pre-deploy Command

در Railway، **Pre-deploy command** روی `npm run dev` تنظیم شده که **اشتباه** است!

**مشکل:**
- `npm run dev` برای development است
- در production باید خالی باشد

**راه‌حل:**
1. در Railway، به بخش **"Deploy"** بروید
2. **"Pre-deploy command"** را پیدا کنید
3. آن را **خالی کنید** یا **حذف کنید**
4. **"Save"** را بزنید

### 2. Start Command

اگر Start command روی `npm run start` است:
- باید `npm start` باشد (بدون `run`)

**راه‌حل:**
1. در Railway، به بخش **"Deploy"** بروید
2. **"Start command"** را بررسی کنید
3. باید `npm start` باشد (نه `npm run start`)
4. اگر اشتباه است، تغییر دهید

---

## ✅ تنظیمات صحیح

### Build:
- **Builder**: Railpack (Default) ✅
- **Build Command**: خالی بگذارید (Railway خودکار `npm run build` را اجرا می‌کند) ✅

### Deploy:
- **Pre-deploy command**: **خالی** (یا حذف کنید) ✅
- **Start command**: `npm start` ✅
- **Port**: 3000 ✅

---

## 🚀 بعد از اصلاح

بعد از اصلاح تنظیمات:

1. Railway به صورت خودکار deploy جدید را شروع می‌کند
2. یا می‌توانید به صورت دستی **"Redeploy"** را بزنید
3. منتظر بمانید تا deploy تمام شود (2-3 دقیقه)
4. Domain را تست کنید: `my-citymap-production.up.railway.app`

---

## 📝 خلاصه تغییرات لازم

1. ❌ **Pre-deploy command**: `npm run dev` → ✅ **خالی**
2. ✅ **Start command**: `npm start` (اگر `npm run start` است، تغییر دهید)
3. ✅ **Port**: 3000 (درست است)

---

**بعد از این تغییرات، پروژه باید به درستی کار کند!** 🚀

