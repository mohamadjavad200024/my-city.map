# راهنمای استقرار با SQLite - City Map

SQLite یک دیتابیس عالی است و می‌توانید از آن در production استفاده کنید! فقط باید از پلتفرم‌های مناسب استفاده کنید.

## ✅ پلتفرم‌های مناسب برای SQLite

### 1. Railway (پیشنهادی - ساده‌ترین)

Railway فایل سیستم پایدار دارد و SQLite را به خوبی پشتیبانی می‌کند.

#### مراحل استقرار:

1. **ثبت‌نام در Railway**:
   - به [railway.app](https://railway.app) بروید
   - با GitHub ثبت‌نام کنید

2. **ایجاد پروژه جدید**:
   - روی "New Project" کلیک کنید
   - "Deploy from GitHub repo" را انتخاب کنید
   - Repository خود را انتخاب کنید

3. **تنظیمات خودکار**:
   - Railway به صورت خودکار Next.js را تشخیص می‌دهد
   - Build Command: `npm run build`
   - Start Command: `npm start`

4. **تنظیم Environment Variables** (در صورت نیاز):
   - `NODE_ENV=production`
   - `PORT=3000` (Railway به صورت خودکار تنظیم می‌کند)

5. **Deploy**:
   - Railway به صورت خودکار deploy می‌کند
   - URL پروژه را دریافت می‌کنید

#### مزایای Railway:
- ✅ فایل سیستم پایدار (SQLite کار می‌کند)
- ✅ رایگان برای شروع
- ✅ استقرار خودکار از GitHub
- ✅ بدون نیاز به تغییر کد

---

### 2. Render

Render هم فایل سیستم پایدار دارد.

#### مراحل استقرار:

1. **ثبت‌نام در Render**:
   - به [render.com](https://render.com) بروید
   - با GitHub ثبت‌نام کنید

2. **ایجاد Web Service**:
   - "New +" > "Web Service"
   - Repository را connect کنید

3. **تنظیمات**:
   - **Name**: city-map (یا هر نامی که می‌خواهید)
   - **Environment**: Node
   - **Build Command**: `npm install && npm run build`
   - **Start Command**: `npm start`
   - **Plan**: Free (یا Paid برای production)

4. **Environment Variables**:
   - `NODE_ENV=production`

5. **Deploy**:
   - روی "Create Web Service" کلیک کنید
   - منتظر بمانید تا deploy شود

#### نکته مهم:
- در Render، فایل‌های دیتابیس در `/opt/render/project/src/database` ذخیره می‌شوند
- ممکن است نیاز باشد مسیر دیتابیس را در production تنظیم کنید

---

### 3. DigitalOcean App Platform

#### مراحل استقرار:

1. به [digitalocean.com](https://digitalocean.com) بروید
2. Apps > Create App
3. GitHub repository را connect کنید
4. تنظیمات:
   - Build Command: `npm run build`
   - Run Command: `npm start`
5. Plan: Basic ($5/ماه) یا Professional

---

## 🔧 تنظیمات کد برای Production

### به‌روزرسانی مسیر دیتابیس (در صورت نیاز)

اگر از Render استفاده می‌کنید، ممکن است نیاز باشد مسیر دیتابیس را تنظیم کنید:

```typescript
// در lib/database.ts
const dbPath = process.env.DATABASE_PATH || path.join(process.cwd(), 'database', 'ads.db')
```

سپس در Environment Variables:
```
DATABASE_PATH=/opt/render/project/src/database/ads.db
```

---

## 📦 آماده‌سازی برای استقرار

### 1. Commit کردن تغییرات:

```bash
git add .
git commit -m "آماده برای استقرار"
git push
```

### 2. بررسی Build:

```bash
npm run build
```

اگر build موفق بود، آماده استقرار هستید!

---

## 🚀 استقرار سریع روی Railway

### روش 1: از طریق وبسایت

1. به [railway.app](https://railway.app) بروید
2. "New Project" > "Deploy from GitHub repo"
3. Repository را انتخاب کنید
4. منتظر بمانید (2-3 دقیقه)
5. ✅ تمام! پروژه شما آنلاین است

### روش 2: از طریق CLI

```bash
# نصب Railway CLI
npm i -g @railway/cli

# ورود
railway login

# در پوشه پروژه
railway init
railway up
```

---

## ⚠️ نکات مهم

### 1. فایل‌های آپلود

برای فایل‌های آپلود شده (تصاویر)، بهتر است از cloud storage استفاده کنید:
- **Railway**: Volume برای فایل‌های پایدار
- **Render**: Volume (در پلن‌های پولی)
- **Cloudinary**: برای تصاویر (رایگان)

### 2. Backup دیتابیس

- Railway: Volume backup خودکار
- Render: می‌توانید فایل دیتابیس را download کنید
- توصیه: Backup منظم از دیتابیس بگیرید

### 3. Environment Variables

هرگز اطلاعات حساس را در کد قرار ندهید:
- از Environment Variables استفاده کنید
- در Railway/Render، Environment Variables را تنظیم کنید

---

## 🎯 مقایسه پلتفرم‌ها

| پلتفرم | SQLite | رایگان | سادگی | مناسب برای |
|--------|--------|--------|-------|------------|
| **Railway** | ✅ | ✅ | ⭐⭐⭐⭐⭐ | همه |
| **Render** | ✅ | ✅ | ⭐⭐⭐⭐ | همه |
| **DigitalOcean** | ✅ | ❌ | ⭐⭐⭐ | Production |
| **Vercel** | ❌ | ✅ | ⭐⭐⭐⭐⭐ | بدون SQLite |

---

## 📞 پشتیبانی

در صورت مشکل:
1. Logs را در پلتفرم بررسی کنید
2. مطمئن شوید `npm run build` موفق است
3. Environment Variables را بررسی کنید

---

## ✨ نتیجه

SQLite یک دیتابیس عالی است و می‌توانید از آن در production استفاده کنید! فقط باید از پلتفرم‌های مناسب استفاده کنید.

**پیشنهاد**: Railway را امتحان کنید - ساده‌ترین و بهترین گزینه برای SQLite است! 🚀

