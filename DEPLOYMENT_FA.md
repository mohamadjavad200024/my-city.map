# راهنمای استقرار آنلاین - City Map

## 🚀 استقرار سریع روی Vercel

### مرحله 1: آماده‌سازی پروژه

1. مطمئن شوید که تمام تغییرات commit شده‌اند:
```bash
git add .
git commit -m "آماده برای استقرار"
```

2. پروژه را به GitHub push کنید (اگر repository ندارید، یکی بسازید):
```bash
git remote add origin https://github.com/YOUR_USERNAME/city-map.git
git push -u origin main
```

### مرحله 2: استقرار روی Vercel

#### روش A: از طریق وبسایت Vercel (ساده‌تر)

1. به [vercel.com](https://vercel.com) بروید و ثبت‌نام کنید
2. روی "Add New Project" کلیک کنید
3. Repository خود را از GitHub انتخاب کنید
4. تنظیمات را به صورت پیش‌فرض بگذارید
5. روی "Deploy" کلیک کنید
6. منتظر بمانید تا استقرار کامل شود (معمولاً 2-3 دقیقه)

#### روش B: از طریق CLI

1. Vercel CLI را نصب کنید:
```bash
npm i -g vercel
```

2. در پوشه پروژه، دستور زیر را اجرا کنید:
```bash
vercel
```

3. دستورالعمل‌های روی صفحه را دنبال کنید
4. برای استقرار production:
```bash
vercel --prod
```

### مرحله 3: تنظیمات مهم

#### ✅ استفاده از SQLite در Production

SQLite یک دیتابیس عالی است و می‌توانید از آن استفاده کنید! فقط باید از پلتفرم‌های مناسب استفاده کنید.

**⚠️ توجه**: Vercel برای SQLite مناسب نیست (فایل سیستم موقت دارد)

**✅ پلتفرم‌های مناسب برای SQLite:**
- **Railway** (پیشنهادی) - ساده‌ترین و بهترین گزینه
- **Render** - رایگان و مناسب  
- **DigitalOcean App Platform** - برای production

**📖 برای راهنمای کامل**: فایل `SQLITE_DEPLOYMENT.md` را ببینید که راهنمای کامل استقرار با SQLite را دارد.

#### راه‌حل‌های پیشنهادی (اگر می‌خواهید از Vercel استفاده کنید):

**1. استفاده از Supabase (PostgreSQL) - رایگان**
- به [supabase.com](https://supabase.com) بروید
- یک پروژه جدید بسازید
- Connection string را دریافت کنید
- کد دیتابیس را تغییر دهید

**2. استفاده از Vercel KV (Redis)**
- در داشبورد Vercel، به Storage بروید
- KV Database بسازید
- برای داده‌های ساده مناسب است

**3. استفاده از Vercel Blob Storage**
- برای فایل‌های آپلود شده (تصاویر)
- در داشبورد Vercel، Storage > Blob

### مرحله 4: تنظیم Environment Variables

در داشبورد Vercel:
1. به Settings > Environment Variables بروید
2. متغیرهای زیر را اضافه کنید (در صورت نیاز):
   - `NODE_ENV=production`
   - `DATABASE_URL` (اگر از PostgreSQL استفاده می‌کنید)

### مرحله 5: بررسی استقرار

بعد از استقرار:
1. URL پروژه را از Vercel دریافت کنید
2. سایت را باز کنید و تست کنید
3. در صورت مشکل، Logs را در Vercel بررسی کنید

## 🔧 استقرار روی پلتفرم‌های دیگر

### Railway

1. به [railway.app](https://railway.app) بروید
2. New Project > Deploy from GitHub
3. Repository را انتخاب کنید
4. Railway به صورت خودکار Next.js را تشخیص می‌دهد
5. PostgreSQL را اضافه کنید (برای دیتابیس)

### Render

1. به [render.com](https://render.com) بروید
2. New > Web Service
3. Repository را connect کنید
4. تنظیمات:
   - Build Command: `npm run build`
   - Start Command: `npm start`
5. PostgreSQL database اضافه کنید

### DigitalOcean App Platform

1. به [digitalocean.com](https://digitalocean.com) بروید
2. Apps > Create App
3. GitHub repository را انتخاب کنید
4. Managed Database (PostgreSQL) اضافه کنید

## 📝 نکات مهم

1. **فایل‌های آپلود**: در production باید از cloud storage استفاده کنید (Vercel Blob, AWS S3, Cloudinary)

2. **دیتابیس**: SQLite فقط برای development است. برای production حتماً از PostgreSQL یا MySQL استفاده کنید.

3. **Environment Variables**: هرگز اطلاعات حساس را در کد قرار ندهید. از Environment Variables استفاده کنید.

4. **Domain**: می‌توانید domain شخصی خود را در Vercel تنظیم کنید.

## 🆘 رفع مشکلات

### خطای Build
- Logs را در Vercel بررسی کنید
- مطمئن شوید `npm run build` به درستی کار می‌کند

### خطای دیتابیس
- SQLite را به PostgreSQL تبدیل کنید
- Connection string را درست تنظیم کنید

### خطای آپلود فایل
- از cloud storage استفاده کنید
- مسیر فایل‌ها را درست تنظیم کنید

## 📞 پشتیبانی

در صورت مشکل، Issues را در GitHub بررسی کنید یا با تیم توسعه تماس بگیرید.

